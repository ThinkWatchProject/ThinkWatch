// ---------------------------------------------------------------------
// Auto-generated OpenAPI TypeScript types — DO NOT EDIT.
//
// Regenerate from a running dev server:
//
//   cd web && pnpm gen:api
//
// `pnpm gen:api` curls `/api/openapi.json` (which utoipa exports from the
// Rust handlers' #[utoipa::path] + ToSchema derives) and pipes it through
// `openapi-typescript`. Route-component types stop drifting from the
// backend the moment you rerun this after a backend change.
//
// This placeholder is committed so imports compile before the first
// generation; run `pnpm gen:api` and overwrite it.
// ---------------------------------------------------------------------

/* eslint-disable @typescript-eslint/no-empty-object-type */
export type paths = {};
export type components = {
  schemas: Record<string, never>;
};
export type operations = Record<string, never>;
